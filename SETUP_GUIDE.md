# 🚀 מדריך הפעלה מהיר

## צעדים להפעלת האתר

### 1. הכנה
```bash
cd /Users/omerbuzaglo/Documents/whatnow/prompt-enhancer
```

### 2. הגדרת API Key

צור קובץ `.env.local`:
```bash
touch .env.local
```

הוסף את ה-API Key:
```
OPENAI_API_KEY=sk-your-key-here
```

💡 **איפה להשיג API Key?**
1. היכנס ל-https://platform.openai.com/
2. לחץ על API Keys בתפריט
3. צור מפתח חדש
4. העתק והדבק ב-.env.local

### 3. הפעלת השרת
```bash
npm run dev
```

### 4. פתח בדפדפן
```
http://localhost:3000
```

---

## ✅ בדיקה שהכל עובד

1. **האתר נטען?** - אמור לראות ממשק יפה עם כותרת "Prompt Enhancer"
2. **כתוב פרומפט פשוט** - לדוגמה: "כתוב מאמר על AI"
3. **לחץ "שפר פרומפט"** - אמור לקבל פרומפט משופר תוך מספר שניות
4. **בדוק את ההסבר** - אמור לראות מה שופר בעברית

---

## 🐛 פתרון בעיות

### שגיאה: "OpenAI API key is not configured"
**פתרון:** וודא שיצרת קובץ `.env.local` עם המפתח

### שגיאה: "Failed to enhance prompt"
**פתרון:** 
- בדוק שיש לך קרדיט ב-OpenAI
- בדוק שהמפתח תקין
- בדוק חיבור לאינטרנט

### האתר לא נטען
**פתרון:**
```bash
# נקה ונסה שוב
rm -rf .next
npm run dev
```

---

## 📝 דוגמאות לניסוי

נסה את הפרומפטים האלה:

1. **פשוט מאוד:**
   ```
   כתוב לי על AI
   ```

2. **בקשת עזרה:**
   ```
   עזור לי עם הקוד שלי
   ```

3. **משימה עסקית:**
   ```
   צור תוכנית שיווק למוצר חדש
   ```

4. **כתיבה יצירתית:**
   ```
   כתוב סיפור קצר
   ```

תראה איך כל אחד מהם הופך לפרומפט מקצועי ומפורט!

---

## 🎯 טיפים לשימוש

1. **היה ספציפי יותר** - ככל שתיתן יותר פרטים, כך התוצאה תהיה טובה יותר
2. **השתמש בהסבר** - קרא מה שופר כדי ללמוד
3. **התאם אישית** - הפרומפט המשופר הוא נקודת התחלה, אפשר לערוך
4. **נסה שוב** - אם התוצאה לא מושלמת, נסה לנסח אחרת

---

**מוכן לשימוש! 🎉**
